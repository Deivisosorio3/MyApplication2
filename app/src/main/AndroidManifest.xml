<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/vetery_Icons"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vista.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vista.RegistroMascota" />
        <activity android:name=".vista.Consultar" />
        <activity android:name=".vista.RegistroVacuna" />
        <activity android:name=".vista.Detalles" />
        <activity android:name=".vista.RegistroPropietario" />
    </application>

</manifest>